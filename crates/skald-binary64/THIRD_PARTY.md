# `skald-binary64` third-party notice

`skald-binary64` uses the following third-party package in the compiler. This
dependency is not linked into generated Skald programs or their runtime.

## `rustc_apfloat`

- Package release: `0.2.3+llvm-462a31f5a5ab` (selected by the exact Cargo
  requirement `=0.2.3`; SemVer build metadata is not part of version matching)
- Source: <https://crates.io/crates/rustc_apfloat/0.2.3+llvm-462a31f5a5ab>
- License: Apache-2.0 WITH LLVM-exception

The exact resolved package and its transitive dependency graph are recorded in
the workspace `Cargo.lock`.
