from std::hash import mix_u64;
from std::lang import Equatable, Hashable;

public class BoxI64 implements Equatable, Hashable {
    final value: i64;

    init(value: i64) {
        self.value = value;
    }

    fn equals(ref other: Obj) -> bool {
        if (!(other is BoxI64)) {
            return false;
        }
        return self.value == ((BoxI64) other).value;
    }

    fn hash_code() -> u64 {
        return mix_u64(((u64) self.value) ^ 0x6936345f626f7801u);
    }
}
