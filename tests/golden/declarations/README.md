# Declaration fixtures

`names.golden.toml` owns duplicate function, external, parameter, and local
bindings plus unknown declared types. Each focused source selects one stable
resolver diagnostic and primary location.

Run this group with `scripts/golden.sh --filter 'declarations/**'`.
