import std::io;

class Resource {
    value: i64;

    init(value: i64) {
        self.value = value;
    }

    destroy {
        std::io::println_bool(self.value);
    }
}

fn main() -> i64 {
    return 0;
}
