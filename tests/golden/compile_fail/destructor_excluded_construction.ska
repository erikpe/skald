class Leaf {
    init() {}
}

class Owner {
    leaf: Leaf;

    init() {
        self.leaf = Leaf();
    }

    destroy {
        self.leaf = Leaf();
        var copy: Leaf = self.leaf;
    }
}

fn main() -> i64 {
    return 0;
}
