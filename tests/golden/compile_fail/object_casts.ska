class Root {
    init() {}
    fn touch() -> unit {}
}

class Unrelated {
    init() {}
    fn touch() -> unit {}
}

fn invalid(ref root: Root) -> unit {
    ((Unrelated) root).touch();
    ((shared Root) root).touch();
}

fn main() -> i64 {
    return 0;
}
