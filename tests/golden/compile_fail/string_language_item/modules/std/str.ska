public class Str {
    private _storage: shared u64[];
    private _start: u64;
    private _length: u64;

    init() {
        self._storage = new u64[]();
        self._start = 0u;
        self._length = 0u;
    }
}
