class Value {
    field: i64;

    init() {
        self.field = 0;
    }

    mut fn set(value: i64) -> unit {
        self.field = value;
    }

    fn write() -> unit {
        self.field = 1;
    }

    fn forward() -> unit {
        self.set(2);
    }
}

fn main() -> i64 {
    return 0;
}
