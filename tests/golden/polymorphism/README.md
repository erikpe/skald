# Polymorphism fixtures

`dispatch.golden.toml` owns inheritance and virtual dispatch behavior plus
hierarchy, override, and exact method-type diagnostics. The native dispatch
trace remains exact external data.

Run this group with `scripts/golden.sh --filter 'polymorphism/**'`.
