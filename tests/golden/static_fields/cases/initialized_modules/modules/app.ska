import std::io;
import state as storage;
from peer import read;

fn main() -> i64 {
    std::io::println_i64(storage::Base.hidden_value());
    std::io::println_i64(storage::Derived.value);
    std::io::println_i64(read());
    return 0;
}
