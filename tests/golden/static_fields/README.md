# Static-field module fixtures

`modules.golden.toml` owns cross-module static-field behavior. Its provider
sources remain undiscovered below `cases/`.

Run this group with `scripts/golden.sh --filter 'static_fields/modules**'`.
