schema = 1

[[test]]
name = "arguments"
mode = "run"
source = "process_arguments.ska"

[[test.run]]
name = "exact_bytes"
argv_file = "data/exact-bytes.argv"
expect = { exit = 0 }

[[test.run]]
name = "no_suffix"
expect = { exit = 0 }
