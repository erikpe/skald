from std::error import panic;

class Resource {
    init() {}

    destroy {
        panic("static shutdown failure");
    }
}

class State {
    static resource: Resource = Resource();
    init() {}
}

fn main() -> i64 {
    return 0;
}
