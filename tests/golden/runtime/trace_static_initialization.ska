from std::error import panic;

fn fail() -> i64 {
    panic("static initializer failure");
    return 0;
}

class State {
    static value: i64 = fail();
    init() {}
}

fn main() -> i64 {
    return 0;
}
