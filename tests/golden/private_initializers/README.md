# Private-initializer module fixtures

`modules.golden.toml` owns foreign construction through an imported class. It
matches the stable diagnostic identity and relative primary location while
allowing richer renderer context.

Run this group with `scripts/golden.sh --filter
'private_initializers/modules/**'`.
