interface Readable {
    fn read() -> i64;
}

class Value {
    init() {}
}

fn excluded(
    no_unit: unit?,
    no_interface: Readable?,
    no_object: Obj?
) -> unit {}

fn main() -> i64 {
    return 0;
}
