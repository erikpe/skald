from std::test import assert_eq_i64;

fn increment(mut ref value: i64) -> unit {
    value = value + 1;
}

fn observe(ref value: i64) -> i64 {
    return value;
}

fn main() -> i64 {
    var value: i64 = 41;
    increment(value);
    assert_eq_i64(observe(value), 42);
    return 0;
}
