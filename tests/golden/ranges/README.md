# Range fixtures

These fixtures cover the canonical successor protocol, ordinary generic range
class, and concise range expressions. Native coverage proves explicit and
concise primitive and class iteration, stored concise ranges, evaluation
order, and loop exits without a new runtime service.

Run this group with `scripts/golden.sh --filter 'ranges/**'`.
