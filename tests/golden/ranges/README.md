# Range fixtures

These fixtures cover the implemented canonical successor protocol before the
ordinary generic range class and concise range syntax land. Native successor
coverage proves the three integer primitive intrinsics, wrapping behavior for
manual maximum-value calls, and ordinary class-witness dispatch without a new
runtime service.

Run this group with `scripts/golden.sh --filter 'ranges/**'`.
