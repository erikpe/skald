class Vec<T> {
    storage: T?[];

    init(length: u64) {
        self.storage = T?[](length);
    }

    fn get(index: i64) -> T? {
        return self.storage[index];
    }
}

fn main() -> i64 {
    var values: Vec<i64> = Vec<i64>(1u);
    var value: i64? = values.get(1);
    return 0;
}
