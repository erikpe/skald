fn target(value: i64) -> i64 {
    var zero: i64 = 0;
    return value / zero;
}

fn argument() -> i64 {
    var zero: i64 = 0;
    return 2 / zero;
}

fn main() -> i64 {
    var callback: fn(i64) -> i64 = target;
    return callback(argument());
}
