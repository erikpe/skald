import dep;
from dep import exported;

fn main() -> i64 {
    var qualified: fn(i64) -> i64 = dep::exported;
    var selected: fn(i64) -> i64 = exported;
    var static_method: fn(i64) -> i64 = dep::Math.apply;
    return qualified(10) + selected(11) + static_method(21);
}
