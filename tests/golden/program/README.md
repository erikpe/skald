# Program-entry fixtures

`entry.golden.toml` owns missing, external, parameterized, and incorrectly
typed `main` declarations. Diagnostics match the entry-contract identity,
message, and primary repository-relative location.

Run this group with `scripts/golden.sh --filter 'program/**'`.
