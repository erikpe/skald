from model import Item;
from std::vec import Vec;

fn main() -> i64 {
    var values: Vec<Item> = Vec<Item>();
    values.push(Item(40));
    values.push(Item(42));
    return values.index_get(-1).value;
}
