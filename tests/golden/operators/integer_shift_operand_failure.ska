import std::io;

fn left() -> u64 {
    std::io::println_i64(1);
    return 8u;
}

fn main() -> i64 {
    var missing: u64? = none;
    var result: u64 = left() >> missing!;
    return (i64) result;
}
