# Post-Proof CFG Canonicalization Discoveries

Status: open for implementation-specific findings from the planned
[post-proof CFG canonicalization roadmap](POST_PROOF_CFG_CANONICALIZATION_ROADMAP.md).

The frozen
[design](POST_PROOF_CFG_CANONICALIZATION_DESIGN_PROPOSAL.md) owns the reviewed
scope: deterministic predecessor-edge facts, permanent-attachment barriers,
independently selectable empty-block forwarding and basic-block merging,
narrow final-stage mutation, default composition, and semantic equivalence.
The
[optimization candidate catalog](OPTIMIZATION_CANDIDATE_CATALOG.md) owns
cross-domain candidate status, placement, effort, value, and pitfalls.

Record only findings that should outlive the implementation task which found
them. Each entry should include concrete evidence, impact, likely owner,
priority, and a bounded later direction. Small maintainability improvements
which support the active task should be implemented directly. Do not widen the
frozen design through this file.

No implementation-specific follow-up has been recorded yet.
