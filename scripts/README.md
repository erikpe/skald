# Scripts

Repository workflow scripts belong here when repeated build, golden-test, or release tasks justify them. Core compiler behavior must remain available through `skac` and Cargo rather than existing only in shell scripts.

